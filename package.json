{
  "name": "esp32-browser",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "postinstall": "PUPPETEER_CACHE_DIR=./.cache npx puppeteer browsers install chrome",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.0.0"
  }
}
